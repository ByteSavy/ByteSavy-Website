/**
 * Data only - re-exported from framer2.js so the main app can import
 * without pulling in Motion/ReactDOM from the standalone demo.
 * Keep in sync with framer2.js BgPaths and springConfig.
 */
export { BgPaths, springConfig } from './framer2.js';
