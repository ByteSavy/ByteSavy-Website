/**
 * ByteSavy theme - single source of truth from company_data.json
 * Use ONLY these colors across the entire app.
 */
export const colors = {
  primary: '#185BCE',        // Primary Blue - buttons, links, highlights
  darkBackground: '#0A1A33', // Dark Navy - main bg, footer, hero
  accent1: '#27E3FF',        // Cyan Accent - hover, highlights, gradients
  accent2: '#A5D4FF',        // Soft Sky - subtle backgrounds, cards
  textLight: '#FFFFFF',      // White - text on dark backgrounds
};

export const typography = {
  primaryFont: 'Poppins',
  secondaryFont: 'Inter',
};

export default colors;
